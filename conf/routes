# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

#Farmer numbers
GET         /renderViewNumbers           controllers.CFarmers.renderViewNumbers()
GET         /numberslist                 controllers.CFarmers.numberslist()

#Customer numbers
GET         /renderViewCustomerNumbers           controllers.CCustomers.renderViewCustomerNumbers()
GET         /custnumberslist                         controllers.CCustomers.custnumberslist()

#MobileApis
POST         /SendingfarmerSMS             controllers.MobileAPI.SendingfarmerSMS()
POST         /SendingcustomerSMS           controllers.MobileAPI.SendingcustomerSMS()